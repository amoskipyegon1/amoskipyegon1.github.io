{
  "name": "amos.me",
  "version": "1.0.0",
  "author": "Amos",
  "license": "ISC",
  "description": "Personal site",
  "source": [
    "src/index.html"
  ],
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "targets": {
    "default": {
      "distDir": "./docs"
    }
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3"
  }
}
