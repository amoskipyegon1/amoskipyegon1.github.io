{
  "name": "amos.me",
  "version": "1.0.0",
  "description": "Personal site",
  "source": [
    "src/index.html"
  ],
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amos",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
